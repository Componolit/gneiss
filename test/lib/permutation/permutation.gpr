
project Permutation is

   for Source_Dirs use ("src");
   for Object_Dir use "build";
   for Create_Missing_Dirs use "True";

   package Builder is
      for Switches ("Ada") use ("-O2");
   end Builder;

end Permutation;
